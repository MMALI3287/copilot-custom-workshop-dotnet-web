---
applyTo: "**/*.cshtml"
---
- 手書きの `site.css` デザインシステムのクラスを使用すること
- レスポンシブデザインを意識し、既存の `site.css` クラスを組み合わせること
- UI ラベルは `@L["キー名"]` でリソースから取得すること（直接記述しない）
- 即時切り替えの対象要素には `data-i18n="キー名"` を付けること
- 猫の名前・説明・品種などのデータには `data-i18n` を付けないこと
- `asp-*` タグヘルパーを積極的に使用すること
