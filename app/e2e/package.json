{
  "name": "meowworld-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Browser tests for MeowWorld: the JA/EN toggle and the CRUD round trip.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
